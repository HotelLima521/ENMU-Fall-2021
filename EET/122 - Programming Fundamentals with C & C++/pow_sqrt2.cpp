#include <iostream>
#include <cmath>

using namespace std;

int main()
{
// Convert the following math expression to C++ expression: (1 + r/100)^n

	int n;
	int r;
	int x = 1+r/100;

	answer = pow(x, n);
	
// Convert the math expression to c++ expression sqrt(a^2+b^2)


	int a;
	int b;

	answer = sqrt(a^2+b^2); 
}
